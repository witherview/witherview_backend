package repository;

import entity.CheckListType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CheckListTypeRepository extends JpaRepository<CheckListType, Long> {
}
